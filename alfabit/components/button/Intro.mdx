import { Meta, Primary, Controls, Canvas } from "@storybook/blocks";

import * as Story from "./Button.stories.ts";

{/* Fazendo um link direto via código */}

<Meta of={Story} />

# Botão

Componente do botão padrão

{/* Primary é um bloco que tá pegando o botao sem variação */}
<Primary />

{/* Canvas dá story de variavaçõ, dando a prorpriedade of, de onde é ssa variação */}
<Canvas of={Story.Violet}/>

{/* Coloca as variáveis e exemplifica mudanças */}
<Controls />
